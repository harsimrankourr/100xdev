Small boi stuff 

1.Tags
2.Attributes 
3.Inline style (bg color, fonts,.)
4.Js basics, data type, loops, fns,....
Big boi stuff
   
1.External  style, classes and ids
2.Flexbox
3.Connecting js to HTML(selectors, DOM)
4.Chrome Dev tools 


The goal of a browser is it needs to allow people to hit a specific server (servers are specified using URL) get back some html,css and java script and render it on the screen 

Browsers are something anyone can built 

Must know the Popular tags and attributes in HTML
<html>
<head>
<title>
<body>
<div>/<span>
<h1>
<p>
<img>
<a>
<input>
<button>
<b><i>      (bold and italic text)
<center>

Difference bt div and span 
In span i will take only as much space as i need (span takes only as much space as it needs).
Div takes a full space 

Attributes 
1.<img src="a.png">
2.<a href="google.com">
3.<button onclick= "">
4.<input id="username">

The term meta data 
Common styling attributes
color
background
border-radius
border
padding/margin
Box-shadow

Padding is ind=side the div and margin is outside to the div and 
Border is in between the margin and the padding 

The dumb way to position the elements on the web page is “float: right”

Right way to position the elements is 
Flex box
It can done that even if i put div’s inside the flex box, they will appear right next to each other 
Syntax:
Justify content: flex-end;
Justify content: space-between;





